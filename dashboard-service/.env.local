# Analytics gRPC service (read API: GetUsageStats, StreamDecisions, GetTopClients)
ANALYTICS_GRPC_URL=localhost:50052

# Config + RateLimit gRPC — served by the unified server/ module on port 9090
CONFIG_GRPC_URL=localhost:9090
RATELIMIT_GRPC_URL=localhost:9090
